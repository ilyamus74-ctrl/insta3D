## Stage 0B server-side verification attempt

The verification could not be executed inside the Codex sandbox because:

- /home/makler/web is unavailable;
- the production database is unavailable;
- the real TGZ is unavailable;
- outbound GitHub access returned HTTP 403.

This does not describe the production server state.

Stage 0 runtime acceptance remains BLOCKED and must be completed
directly on the actual web server.